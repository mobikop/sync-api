apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-chart.fullname" . }}-service
  labels:
    {{- include "user-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: web
      protocol: TCP
  selector:
    {{- include "user-chart.selectorLabels" . | nindent 4 }}
